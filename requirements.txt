# Full dependency pin list: core + all optional extras from pyproject.toml
# (browser, transcribe, server, youtube, gui, dev, build-gui). Install: pip install -r requirements.txt

# --- core ---
aiohttp>=3.9.0
aiofiles>=23.2.1
aiosqlite>=0.19.0
rich>=13.7.0
pyyaml>=6.0.1
python-dateutil>=2.8.2
gmssl>=3.2.2

# --- optional: browser ---
playwright>=1.40.0

# --- optional: transcribe ---
openai-whisper>=20231117

# --- optional: server ---
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0

# --- optional: youtube ---
google-api-python-client>=2.0
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2

# --- optional: gui ---
PySide6>=6.5

# --- optional: dev ---
pytest>=7.0
pytest-asyncio>=0.21
ruff>=0.4.0
httpx>=0.25

# --- optional: build-gui (PyInstaller) ---
pyinstaller>=6.0
